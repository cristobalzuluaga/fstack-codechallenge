name: build app

on:
  push:
    branches:
      - "feature/**"

jobs:
  build-app:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 15
    - names: install dependencies
      run: npm install
    - names: running lints
      run: npm run lint
    - names: running prettier
      run: npm run prettier
    - names: running unit tests
      run: npm run test
    - names: compile frontend
      run: npm run build
